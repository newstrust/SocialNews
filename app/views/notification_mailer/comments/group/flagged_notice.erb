A comment on the "<%= @record.commentable.name %>" group page was flagged:

<%= render :partial => 'notification_mailer/comments/flagged_details', :locals => { :permalink => "#{comment_permalink_for(@record)}" } %>
