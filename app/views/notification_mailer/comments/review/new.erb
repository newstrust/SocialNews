Hi <%= first_name(@to) %>,

<%= StringHelpers.linewrap_text("#{@record.member.display_name} commented on your review for this story on #{app_name}:",72) %>

---------------------------------------------------------------------

"<%= @review.story.title %>"
<%= strip_tags link_to_sources(@review.story.sources) -%>
<%= " - by #{@review.story.journalist_names}" unless @review.story.journalist_names.blank? %>
<%= "#{@review.story.story_date.strftime('%b. %e, %Y')}" unless @review.story.story_date.blank? -%>
<%= " (#{humanize_token(@review.story, :story_type)})" if @review.story.story_type %>

Here's <%= first_name(@record.member) %> comment:

Comment:
"<%= h(strip_tags(StringHelpers.linewrap_text(StringHelpers.truncate_on_word_boundary(@record.body_plain,300,400,true),72))) unless @record.body_plain.blank? %>"

---------------------------------------------------------------------

To see all comments for this story, click here:
<%= story_url(@review.story, :only_path => false) %>

<%= first_name(@record.member) %>'s full comment can be viewed here:
<%= comment_permalink_for(@record) %>

To add a comment on this page, click "Add a Comment."

To view <%= first_name(@record.member) %>'s profile, click here:
<%= member_url(@record.member.to_param) %>

<%= render :partial => 'footer' %>
