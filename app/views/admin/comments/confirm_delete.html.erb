<h3>Delete this comment</h3>
<%= render :partial => 'comment', :locals => { :comment => @comment, :no_buttons => true } %>
<% form_for(@comment, :url => admin_comment_path(@comment), :html => { :class => 'comment_form', :method => :delete }) do |f| -%>
<p>
	<%= submit_tag "Hide Comment" %>
 	<%= cancel_tag "/admin/comments" %></p>
<%- end -%>
